<nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top bg-white border-bottom" style= "border-color: #afafaf;">
  <div class="container-fluid w-75">
    <%= link_to root_path, class:"navbar-brand" do %>
        <%= image_tag "logo.png", width:"100rem"%>
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="d-flex">
        <div class="collapse navbar-collapse me-5" id="navbarSupportedContent">
        <form class="d-flex">
            <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search" style="background: #efefef">
        </form>
        </div>

        <div class="gap-4 navbar-collapse">
            <%= link_to root_path, class:"text-dark" do %><i class="fa-solid fa-house fa-xl"></i><% end %>
            <i class="fa-brands fa-facebook-messenger fa-xl"></i>
            <!-- Button trigger modal -->
            <a type="button" class="" data-bs-toggle="modal" data-bs-target="#exampleModal">
              <i class=" text-dark fa-regular fa-square-plus fa-xl"></i>
            </a>
            <i class="fa-regular fa-compass fa-xl"></i>
            <i class="fa-regular fa-heart fa-xl"></i>


            <div class="dropdown">
              <a class="" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                <% if current_user && current_user.profile_pic.attached? %>
                  <%= image_tag current_user.profile_pic, class:"img-thumbnail rounded-circle", style:"width: 2rem;"%>
                <% else %>
                  <%= image_tag "profile-default", class:"img-thumbnail rounded-circle", style:"width: 2rem;"%>
                <% end %>
              </a>
              <ul class="dropdown-menu">
                <li>
                  <%= link_to "Profile", user_path(current_user), class:"dropdown-item" %>
                </li>

                <li>
                  <%= link_to "Settings", edit_user_registration_path, class:"dropdown-item" %>
                </li>
                <li>
                  <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class:"dropdown-item" %>
                </li>

              </ul>
            </div>
        </div>
    </div>
  </div>
</nav>

<%= render "posts/form", post: Post.new %>